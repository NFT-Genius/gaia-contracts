{
    "accounts": {
        "storefront-testnet": {
            "address": "${STOREFRONT_ADDRESS}",
            "key": "${STOREFRONT_PRIVATE_KEY}"
        },
        "gaia-testnet": {
            "address": "${MINTER_ADDRESS}",
            "key": "${MINTER_PRIVATE_KEY}"
        }
    },
    "deployment": {
        "testnet": {
            "gaia-testnet": [
                "Gaia"
            ],
            "storefront-testnet": [
                "NFTStorefront"
            ]
        }
    }
}